# /usr/bin/sh
# script to link all image files to images folder for data analytics
# created on 4/9/22

c=0
cd images
for i in $(find '../' -name *.jpeg)
do
  echo $i
  ln -s .$i .
  ((c+=1))
  if ((c==10))
  then
    exit 0
  fi
done;
